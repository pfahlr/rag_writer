# Content Types Configuration
# Supports token replacement using {{variable_name}} syntax
# Available tokens: book_title, chapter_title, section_title_hierarchy, subsection_title, subsection_id, target_audience, topic, section_title, num_prompts, rag_context, current_date

# Individual content type configurations are now stored in separate files in the content_types/ subdirectory
# Each content type has its own YAML file named after the content type (e.g., technical_manual_writer.yaml)

# Available content types:
# - pure_research
# - technical_manual_writer
# - science_journalism_article_writer
# - folklore_adaptation_and_anthology_editor

